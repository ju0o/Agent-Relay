# AR-GH-PM-AUTO — implementation spec (from ju0o/Agent-Relay#3; PM-authored 2026-09-16)

Attempt 2 = implement GitHub issue ju0o/Agent-Relay#3 (read it FIRST: `gh api repos/ju0o/Agent-Relay/issues/3 --jq .body`; every section binding: PM_PACKET v1 fields, Work prompt contract, importer safety 1–13, automatic continuation, disposable certification list). Worktree /home/skkse12/Desktop/Projects/Core/Agent-Relay-ghpm, branch ar/github-pm-bridge-v0 (AR-01+AR-04 base). Build `src/github-pm/` compiled by build:server to dist/server/github-pm/main.js.
TRANSPORT (already provisioned by the PM; do not create branches/PRs): repo ju0o/Agent-Relay, wake PR #5 (head pm-transport-wake, base pm-transport-base), packets at pm-bridge/inbox/<packet_id>.json, one commit per packet pushed to pm-transport-wake; a local clone for pushing exists at ~/.local/share/agent-relay-pm-transport/repo (on pm-transport-wake). Judgments arrive as PR #5 comments (`PM_JUDGMENT v1` with decision ACCEPT|CHANGES|OWNER_REQUIRED, retry NONE|SAME_TASK, packet_id, context_hash, reason); the importer replies `PM_IMPORT v1` as a PR comment (comments never trigger the wake).
CLI: `--dataRoot --project (repeatable allowlist) --transport git-gh|local --repo-dir <clone> --pr owner/repo#5 --state-file --audit-dir --once|--poll-ms --mode export|import|both --dry-run`. `git-gh` transport: packet commit+push via `git` in --repo-dir (author "Agent Relay PM Bridge"), comments via `gh api repos/{o}/{r}/issues/{pr}/comments` (GET since / POST) — the code never handles tokens. `local` transport for tests: a local bare git repo as origin + a JSON comment store directory.
EXPORTER: for each pending TASK_VERIFY delivery in allowlisted projects build the bounded context from existing backend modules only (goal-task, run/result, pm-verification-context, evidence, pm-judgment history) → context_hash sha256 → packet JSON {schema_version:'pm-packet.v1', packet_id, project, task_id, run_id, delivery_id, result_id?, created_at, bounded_context_hash, allowed_actions (ACCEPT, CHANGES always; RETRY_SAME_TASK only when canonically legal), canonical_state_summary, verification_context (bounded, no secrets/full logs/other projects), evidence_refs, retry_lineage/attempt} → write pm-bridge/inbox/<packet_id>.json, commit "PM_PACKET <task_id> <packet_id>", push; exactly one packet per (delivery, context_hash) via state-file; changed context while pending → new packet + SUPERSEDED note in the previous one's follow-up comment; duplicate polls push nothing.
IMPORTER: poll PR comments since last id; parse only strict PM_JUDGMENT v1; apply steps 1–13 from the issue (schema, re-read CURRENT canonical, recompute hash, equality, pending Delivery still current, stale run/result rejected, id mismatch rejected, action ∈ allowed_actions, non-identical replay rejected, byte-identical replay = idempotent no-op, existing APIs only: pm-judgment.submitPmJudgment and for CHANGES+SAME_TASK retry-preparation.prepareRetryForJudgment; OWNER_REQUIRED → no canonical write, audit + PM_IMPORT reply OWNER_REQUIRED_RECORDED); audit JSONL {ts, packet_id, github comment id/url, event ids if known, project/task/run/delivery/result ids, context_hash, decision, retry, disposition APPLIED|REJECTED_STALE|REJECTED_INVALID|REPLAY_IGNORED|OWNER_REQUIRED_RECORDED|FAILED, resulting canonical state}; restart-safe (record write before reply; resume finishes reply only); GitHub outage → no canonical change; failures leave the packet pending and recoverable.
TESTS `test/github-pm-bridge.test.mjs` (disposable dataRoot + local transport; mint Task→Run→Result→pending Delivery with existing helpers as in test/v1-g5a-pm-judgment.test.mjs): exactly-one packet per delivery; duplicate poll no dup; simulated CHANGES judgment → applied + SAME Task retry preparation, no new Task; second Result → second packet; ACCEPT → terminal; duplicate commit/poll dedupe; duplicate comment dedupe; stale hash rejected; malformed/malicious judgment rejected (wrong ids, disallowed action, tampered hash); byte-identical replay no-op; importer restart mid-flow no duplicate; exporter restart no duplicate packet; transport outage (unreachable origin) → canonical untouched; zero writes under the live dataRoot (marker + find excluding V02CControlTower/). Paste the output.
RULES: this worktree only; never touch the deployment tree, live dataRoot, units, tmux, cloudflared; no OpenAI API; the git-gh transport is not exercised in tests beyond `--dry-run` printing exact git/gh commands. Commit on ar/github-pm-bridge-v0 with scoped git add (src/github-pm, test file, package.json script, docs/GITHUB_PM_AUTO_WAKE.md incl. the exact Founder one-time Work trigger conditions and the Work prompt contract text, and the PM's run commands for disposable-real and live) — never amend; report hash + `git diff --stat 93506a0..HEAD`.
