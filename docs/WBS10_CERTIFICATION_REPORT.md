# WBS-10 — Real one-project certification report (issue #6) — PM draft for independent review

Project: `V1CERT` (disposable-real; workspace `~/Desktop/Projects/Core/v1cert-workspace`, git). Branch `ar/github-pm-bridge-v0`, HEAD `f02f78f` (orchestrator code up to `0180c61`; later commits are docs). Runbook: `docs/WBS10_RUNBOOK.md` (26 `--once` passes, 09:28Z–11:26Z). Audit: `.jucontroler-staging/wbs10/audit/*.jsonl`.

| Field (issue #6) | Value |
|---|---|
| Runtime PM adapter / auth mode | `opencode-command` → OpenCode 1.18.27 `serve` on 127.0.0.1:4111 (Basic auth from a 0600 password file, never logged), model `opencode/mimo-v2.5-free`, fallbackChain `[opencode/big-pickle, opencode/nemotron-3-ultra-free]`, permissionProfile read-only, tools disabled by explicit map. No OAuth login was needed. |
| Extra paid API dependency | **ZERO** — OpenCode free models for PM; Builder = existing Codex seat (`w-v1cert-codex-001`, pane %16, actl-managed); QA = existing Claude Code worker (`claude-code`, default profile). No OpenAI API, no new credentials. |
| TASK_CONTRACT hash | TASK-0002 `a7c48a6e81744cce36a38a9ccfc7e8e70613a37679c9a11e480acd96c42272cf` (contract_revision 1; created by the PM runtime at pass 18 after one validation re-ask) |
| Builder attempt 1 | run `f2949457-eb99-4481-a050-ed6bd8f45a6f` — dispatched pass 20 through the permit-checked actl path; Codex delivered scripts/check.mjs, docs/OPERATIONS.md, CHANGELOG.md; Result collected (`RUN_RESULT_RECEIVED`) |
| QA result 1 | `QA-TASK-0002-f2949457…` **FAIL** — deterministic AC-05 diffScope (untracked dirs `docs/`, `scripts/` reported vs exact allowed files); QRP `QRP-QA-TASK-0002-f2949457…` minted |
| Builder attempt 2 | run `c33d25e3-84eb-4ff8-b29a-3a7736739b5e` — same-Task remediation #1, dispatched pass 23 after `SEAT_RELEASED` of the source run; Codex restaged exactly the three contract files |
| QA result 2 | `QA-TASK-0002-c33d25e3…` **PASS** — deterministic PASS, semantic PASS (Claude Code QA worker); pending TASK_VERIFY Delivery `PMD-TASK-0002-c33d25e3…` |
| PM judgment | `PMJ-PMD-TASK-0002-c33d25e3…` **ACCEPT**, applied automatically at pass 24 (11:25:04Z); Delivery ACKNOWLEDGED |
| Final Task state | TASK-0002 `executionState RESULT_RECEIVED`, `pmState ACCEPTED` |
| Next Task state | none — PM bootstrap decided `PROJECT_COMPLETE` (goal delivered; container goal internal). Truthful stop per issue #6 ("authorized next Task automatically dispatched OR project stops truthfully"). |
| Restart / idempotency drills | Test suites: `v1-builder-dispatch-drills` 5/5, `v1-orchestrator-drills` 8/8, `v1-orchestrator` 35/35, `v1-orchestrator-actl-dispatch` 6/6, `v1-qa-loop` 4/4, `b15-fix-01/03` 2/2. Live: every stage boundary of this run was crossed by a separate `--once` process (restart between judgment→dispatch, Builder→QA, QA→PM); duplicate PM judgment (REPLAY_IGNORED), stale hash (REJECTED_STALE), stuck session (abort+rotate), infra-failed run (Delivery + CHANGES retry), exhausted Task (TASK_EXHAUSTED → replacement) all observed live and fail-closed; passes 25–26 after completion created no new canonical records. |
| Founder intermediate routing | **0** after initial runtime setup (role config, worker record, Goal creation by PM operator). Copy/paste 0, manual wake 0. |
| P0 / P1 defects | Orchestrator range `9af10aa..0180c61`: P0 0, P1 0 open (Codex review #1 P1s closed and certified by Grok; Claude Pro series review PASS_WITH_CONDITIONS, no P0/P1). Pre-existing on the branch before V1 work: `test/v1-g4a-pm-delivery.test.mjs` and `test/v1-g4c-*` red (MCP pm-tools stale-delivery semantics) — classified P1-pre-existing, routed as a separate fix (TASK-0070), not introduced by V1. |
| Open P2 / known limitations | expired-lease retry re-dispatch (consumed preparation) not automated; PROJECT_COMPLETE re-asked once per idle cycle until cached; diffScope evaluator collapses untracked directories; disabled tool-id list is a static allowlist; v02 driver checkout carries an uncommitted dispatcher addition (backlog). |

Defects found on the real runtime and fixed during WBS-10 (each with tests): 17 — tools:{} not disabling tools; PM output contract never delivered; stuck session reuse; qa_route schema; validation convergence; builder selector vs strict registry; actl-bridge missing from the V1 branch (merge of `staging-merge`); input-permit factory; infra-failed run invisible; deterministic-hash misread (PM echo error); retry-run re-mint loop; unsent retry adoption (partial); silent cycles / exhausted Task; actl INPUT_STATE_UNKNOWN idle semantics; trailing-blank-rows; QRP dispatch; EXPIRED_HELD seat before remediation.

PM position: the WBS-10 observed chain is complete; certification (`AGENT_RELAY_ROLE_ORCHESTRATOR_V1 = CERTIFIED`) is proposed to the Owner **only after** the independent certification review of this report and of commits `68bace9..0180c61` returns no P0/P1, and with the pre-existing red tests either fixed or explicitly accepted by the Owner as out of V1 scope.

## Addendum 11:36Z (after the draft was handed to review)
- Pre-existing red suites resolved (`3dee698`): `v1-g4a-pm-delivery` encoded semantics superseded by `be7c41c` (superseded current-attempt Delivery → IGNORED) and was updated with a citing comment, no production change; `v1-g4c` is green when suites run serially (earlier red = concurrent full-suite interference). Branch suites: 97 passed / 0 failed in the Builder's serialized run; PM re-ran g4a/g4c/orchestrator/qa-loop/b15 green. The P1-pre-existing row above is therefore CLOSED.

## Addendum 11:43Z — independent certification review (`docs/WBS10_CERT_REVIEW_CLAUDEPRO.md`)
- Verdict PASS_WITH_CONDITIONS; CHAIN_OBSERVED YES; FOUNDER_ROUTING 0; PAID_API ZERO; P0 none.
- Reviewer reclassified three listed limitations as **P1**: (a) diffScope evaluator collapses untracked directories → a wrong QA FAIL on the real run; (b) a prepared retry whose lease expired before send is still silent in one state; (c) PROJECT_COMPLETE is re-asked as a real PM turn every idle cycle. All three are being fixed in TASK-0071 (`CHANGES round 18`); certification is proposed only when that fix is verified and re-reviewed (P0 = P1 = 0).
- Reviewer P2s accepted: defect count corrected to 17; g4a/g4c are green (addendum 11:36Z); `assertRetryWorkerRecord` raw fallback and the `'restart-check'` snapshot literal are documented as non-security-relevant by trace; the concurrent-run test flake is harness lock hygiene (serial run 0 failures).

## Addendum 11:46Z — P1 closure (`df7523d`)
- (a) diffScope untracked-dir collapse → fixed (`git status -uall`), test `v16-slice2-deterministic-qa` (allowed untracked file PASS / extra file FAIL); (b) expired-lease consumed retry → audited `BLOCKED_RUNTIME` every cycle (live pass 27), test in `v1-g5c-auto-redispatch`; (c) PROJECT_COMPLETE cached by contextHash → live pass 28 `PROJECT_COMPLETE_CACHED`, test in `v1-orchestrator`. Serial suites 97/0. Independent closure review requested (Codex builder10).
