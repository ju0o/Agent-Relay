# Independent review #1 of WBS-5/8 (bed5ed6) — Codex builder6 seat, read-only, 2026-09-16 07:58Z

PM disposition: FAIL accepted as CHANGES to the Builder (Claude Pro). P0 none. Fix order: P1-1 no blind re-send after a PM timeout (interrupt → collect-only → else BLOCKED_RUNTIME; next cycle may re-ask, protected by context_hash); P1-2 primary adapter resolved through the registry by the assignment's runtimeAdapterId and `zeroExtraBilling:false` → OWNER_REQUIRED, never a bypass; P1-3 default dispatch hook selects the worker whose workerId matches the builder assignment's `actl-managed:<workerId>`, fail closed otherwise; P2 timers cleared, fake-builder paths passed explicitly by the test, predicate documented. Certification review of the fixed commit stays independent (Codex or Grok).

WBS58_REVIEW: FAIL

P0:
- NONE. I found no direct canonical filesystem write and no path that applies a judgment without the final canonical/context checks, subject to the P1 conditions below.

P1:
- Timeout recovery sends the same PM envelope again after a best-effort `interrupt()`; if the first request completed or its side effect was accepted but `collect()` timed out, the retry can create a second PM-side effect/session request (`src/orchestrator/role-loop.ts:119-139`). The timeout test only uses a never-resolving fake and cannot prove duplicate-effect safety (`test/v1-orchestrator.test.mjs:497-510`).
- The primary PM adapter is accepted as `cfg.pmAdapter` after only the model guard; it is not checked against a registered adapter or the assignment id. A hand-built config can therefore send through an unregistered injected adapter, and `zeroExtraBilling === false` explicitly bypasses the free-model guard without `OWNER_REQUIRED` (`src/orchestrator/role-loop.ts:80-103`). Validated V1CERT config uses `mimo-v2.5-free`, `opencode/big-pickle`, and `opencode/nemotron-3-ultra-free`, which pass the predicate (`~/.local/share/AgentRelay/data/_relay/roles/V1CERT.json:4-52`).
- The default dispatch hook selects the first registry record tagged `role: 'implementation'`, independently of the Builder assignment's runtime/worker identity. With more than one implementation worker this can dispatch to the wrong seat; only the workspaceRoot comes from the Builder assignment (`src/orchestrator/main.ts:78-88`; deterministic ordering in `src/backend/worker-registry.ts:372-386`). The current read-only registry has `w-v1cert-codex-001` as the observed V1CERT implementation record (`~/.local/share/AgentRelay/data/_relay/workers/w-v1cert-codex-001.json:1-20`).

P2:
- `isFreeTierModel()` is a deliberately brittle suffix/allowlist heuristic: it recognizes only `*-free` and `big-pickle`, not a registry capability or provider policy (`src/orchestrator/role-loop.ts:45-49`).
- `timeoutAfter()` timers are not cleared after `Promise.race()` settles. The 17 tests all emitted `ok`, but the test process remained alive beyond the 30-second command window because successful calls leave long timeout handles (`src/orchestrator/role-loop.ts:111-117`; test run result below).
- The test suite leaves `.builder-attempt` and `out.txt` in its current working directory after the run. The fake Builder defaults both paths from `process.cwd()` (`scripts/fake-builder-worker.mjs:2-6`), and the test invokes it through `spawnSync` (`test/v1-orchestrator.test.mjs:140-151`). The observed files were present at the Agent Relay repository root after the permitted test run.

TESTS_RUN: 17/0
SECRETS: NONE
DIRECT_CANONICAL_WRITES: NONE

| # | Check | Verdict | Evidence / finding |
|---|---|---|---|
| 1 | Canonical writes only through backend APIs | PASS | The only `fs` writes in `src/orchestrator` are the per-cycle state file (`role-loop.ts:182-187`) and JSONL audit (`role-loop.ts:189-192`). Canonical mutations route through `createTask`, `transitionTaskExecution`, `dispatchHook`, `submitPmJudgment`, `prepareRetryForJudgment`, and `reconcileReadyRetryDispatches` (`role-loop.ts:315-342,347-380,478-491`). No direct `_relay/tasks`, `pm-deliveries`, `pm-judgments`, or `qa-*` write exists. |
| 2 | Fail-closed final-gate order | PASS_WITH_CONDITIONS | The packet is rebuilt after PM response and context hash is checked before contract hash and judgment (`role-loop.ts:453-479`). The packet hash includes current canonical context, retry lineage/newer-run information (`pm-packets.ts:128-172`; `docs/WBS4_8_SPEC.md:24-30`). There is no separate explicit newer-run branch; it is covered by the context hash construction and should remain regression-tested. |
| 3 | Billing guard and fallback walker | FAIL | Real V1CERT model refs pass the free predicate, and paid/unregistered fallback entries are skipped (`role-loop.ts:80-105`). However, an injected unregistered primary adapter is not resolved/verified, and a hand-built assignment with `zeroExtraBilling:false` bypasses the guard (`role-loop.ts:60-67,80-103`). |
| 4 | Timeout / duplicate PM side effect | FAIL | After timeout, `interrupt()` is best effort and the identical envelope is sent again (`role-loop.ts:119-139`). A late first completion could overlap the retry; the fake only hangs forever and cannot test that race (`test/v1-orchestrator.test.mjs:497-510`). |
| 5 | Idempotency and restart | PASS | Canonical Delivery status is checked before packet/send (`role-loop.ts:383-391`), and final application is gated by a fresh packet/context and contract hash (`role-loop.ts:453-471`). A deleted or stale state file can lose the local blocked-cache optimization, but cannot bypass the canonical `ACKNOWLEDGED` replay check. |
| 6 | ACCEPT_AND_NEXT scope and freeze | PASS_WITH_CONDITIONS | Next project must equal `cfg.project` and the current Task Goal must equal the V1 container Goal before `createTask` (`role-loop.ts:347-369`). Contract validation occurs before creation; a freeze error propagates rather than mutating an existing dispatched Task (`role-loop.ts:357-360`; `task-contract.ts:249-285`). |
| 7 | PM instructions | PASS | Exactly one fenced JSON block is required (`docs/PM_ROLE_INSTRUCTIONS.md:8-25`); scope expansion, second Tasks, credentials, and Owner-required conditions are defined (`docs/PM_ROLE_INSTRUCTIONS.md:74-100`). It does not instruct the PM to run tools. |
| 8 | Test reality | PASS_WITH_CONDITIONS | Tests assert canonical Task, Delivery, Judgment, retry, scope, and mutation outcomes (`test/v1-orchestrator.test.mjs:181-224,231-426,430-510`). Test 1, packet bounded/hash stability, would still pass if the final-gate guarded apply behavior were removed (`test/v1-orchestrator.test.mjs:170-178`). Main CLI/default dispatch selection is explicitly not unit-tested (`docs/WBS58_EVIDENCE.md:53-59`). |
| 9 | Secrets / provider config endpoint | PASS | Orchestrator code does not log credentials or call `/config/providers`; the adapter reads a password file without logging its contents (`src/integrations/opencode/command-adapter.ts:156-172`). The repository review evidence also records no `/config/providers` call (`docs/V1_REVIEW1_GROK.md:58-65`). |
| 10 | Default dispatch hook seat selection | FAIL | It chooses the first `role: 'implementation'` worker and pairs it with the Builder assignment workspaceRoot, without matching worker identity to `runtimeAdapterId` (`src/orchestrator/main.ts:78-88`). Current V1CERT has one observed implementation worker, so the present selection is deterministic but not multi-seat safe. |
| 11 | Test hygiene | FAIL | The fake Builder writes default `.builder-attempt` and `out.txt` from `process.cwd()` (`scripts/fake-builder-worker.mjs:2-6`). The test runs it with a temporary workspace cwd, but the observed repository root also contained both artifacts after the suite; cleanup only removes the temp `ROOT` (`test/v1-orchestrator.test.mjs:140-151,513-521`). |
