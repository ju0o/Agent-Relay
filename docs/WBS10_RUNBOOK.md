# WBS-10 — real one-project certification runbook (project V1CERT) — draft, PM-operated

Preconditions (all done): workspace git repo `~/Desktop/Projects/Core/v1cert-workspace`; Codex Builder seat `builder9` (`%16`) with worker record `_relay/workers/w-v1cert-codex-001.json`; role config `_relay/roles/V1CERT.json` (pm → `opencode-command` `mimo-v2.5-free`, fallback free models; builder → actl-managed worker; qa → `qa-worker:claude-code`); OpenCode `serve` on 127.0.0.1:4111; WBS-3 proof PASS on the PM model.

Steps
1. Build: `npx tsc -p tsconfig.server.json` in the V1 worktree. Snapshot canonical counts for V1CERT (tasks/qa-attempts/pm-deliveries/pm-judgments) and record `ls ~/.local/share/AgentRelay/data` mtimes for the zero-foreign-write check.
2. Bootstrap: `node dist/server/orchestrator/main.js --dataRoot ~/.local/share/AgentRelay/data --project V1CERT --role-config ~/.local/share/AgentRelay/data/_relay/roles/V1CERT.json --once` with a goal whose PM_BOOTSTRAP yields ONE low-risk Task (real: add `docs/OPERATIONS.md` + `scripts/check.mjs --self-test` to v1cert-workspace, with a qaContract that requires both the self-test exit 0 AND a CHANGELOG entry). Record the TASK_CONTRACT hash.
3. Loop: run `--poll-ms 30000` in the PM tmux window `v1cert-orch` (own window, explicit socket not needed — same server as the PM seat; never touch other panes). Watch canonical only; Founder actions must stay 0.
4. Expected chain: Builder attempt 1 → Result 1 → automatic QA. If QA returns CHANGES, the same-Task remediation Run (QRP) is attempt 2 → Result 2 → QA PASS → pending TASK_VERIFY Delivery → PM final gate auto-invoked → ACCEPT (or CHANGES) → Delivery reconciled → authorized NEXT Task dispatched or truthful stop. If QA passes on attempt 1, the CHANGES leg is NOT observed: report it as NOT_PROVEN for that leg and run a second Task; never force a fake failure.
5. Drills on the live disposable loop: kill the orchestrator between QA PASS and PM gate, restart → no duplicate judgment; restart OpenCode `serve` mid-loop → session resumed; stop the Builder runtime (actl, own seat) → BLOCKED preserved, resume after restart.
6. Report `docs/WBS10_CERTIFICATION_REPORT.md` in the issue #6 format (runtime PM adapter/auth mode, extra paid API ZERO/NONZERO, contract hash, attempts, QA results, PM judgment, final/next Task state, drill results, P0/P1), then independent review (Grok) before the PM proposes `AGENT_RELAY_ROLE_ORCHESTRATOR_V1 = CERTIFIED` to the Owner.

## Pass 1 — 2026-09-16 08:28Z (orchestrator 73f71f2, PM mimo-v2.5-free) → BLOCKED_RUNTIME, canonical untouched
- Goal `GOAL-0001` (V1CERT 운영 점검 스크립트) created; `--once` → bootstrap packet sent; PM session `ses_f56aac22…` never completed: assistant parts `[step-start, reasoning, tool:read]`. The adapter's `tools: {}` does NOT disable OpenCode tools (empty map = no overrides); the model tried to `read` and the request hung until the bounded timeout → audit `BLOCKED_RUNTIME`, `pendingReask.bootstrap` recorded, zero canonical writes (tasks/qa/deliveries/judgments all 0). Fail-closed behaviour confirmed on a real runtime.
- PM probe: explicit all-false tool map → reply in 3.3 s (mimo) / 47 s (big-pickle); both produce the single fenced JSON block when told they have no tools. Fix routed to the Builder (Codex, worktree lane): explicit disabled tool map constant, 'No tools' PM instructions, re-ask sentence on tool-call markup, tests.

## Pass 2 — 08:38Z (2b730f8, tools disabled) → BLOCKED_RUNTIME again: the persistent role-session still pointed at the stuck pass-1 session (its `read` tool part 'running' forever); reusing it timed out. PM aborted it via API and archived `_relay/role-sessions/V1CERT/pm.json` (→ `.jucontroler-staging/wbs10/rotated/`). Defect: no session rotation after timeout.
## Pass 3 — 08:41Z (fresh session) → BLOCKED (no canonical writes): the PM replied within seconds but with its own ```json schema; the parser requires the first line inside the fence to be `PM_TASK_DECISION v1`. Root cause: `docs/PM_ROLE_INSTRUCTIONS.md` is never sent to the PM runtime and packets carry no output contract (the fake PM adapter in tests already knew the format). Fix routed to the Builder (TASK-0054): code-generated output contract appended to every packet + role preamble once per new session + abort/rotate on timeout.
Canonical after passes 1–3: tasks 0 / qa 0 / deliveries 0 / judgments 0 — every failure was fail-closed.
## Pass 4 — 08:53Z (943a7e7): returned in 0 s — the orchestrator-local `blocked` cache keyed by the unchanged contextHash refused to re-ask (by design: no re-ask on an unchanged packet). Operator cleared the local state file (not canonical); follow-up adds `--retry-blocked`.
## Pass 5 — 08:54Z: PM (mimo-v2.5-free) returned a parseable `PM_TASK_DECISION v1` CREATE_TASK in 22 s (contract + preamble delivered). createTask rejected the contract: `deterministic[0] 항목이 잘못되었습니다` — qa_route.deterministic given as strings, semantic.qaWorkerId invented (`opencode`). Zero canonical writes. Fix routed (TASK-0055): full qa_route/task_contract schema rendered from code, qaWorkerId injected from the role config, one re-ask with the validation error, `--retry-blocked`.
Builder seat `%16` was sitting on Codex's directory-trust dialog for the new workspace; PM accepted it (own seat, own disposable repo).
## Pass 6 — 09:02Z (7d4e8cf, --retry-blocked): PM produced a full contract; validation round 1 `deterministic[0].cwd는 workspace 상대 경로여야 합니다`, single re-ask → `DETERMINISTIC 기준 AC-05에 귀속된 deterministic check(criterionId)가 없습니다` → BLOCKED, zero canonical writes. The free model converges one rule per round; fix routed (TASK-0056): bounded validation re-ask (3 rounds, audited) + explicit cwd/criterion/command rules in the rendered contract.
## Pass 7 — 09:08Z (44f5065): MILESTONE — PM (mimo-v2.5-free) produced a valid TASK_CONTRACT after one validation re-ask (`VALIDATION_REASK 1/3`); canonical `V1CERT/TASK-0001` created with contract_hash `077138fb…` (READY, 0 runs). Dispatch hook then failed: `builder worker selection is missing for actl-managed:w-v1cert-codex-001` → no Run created; Task remains READY for the next cycle (idempotent path). Diagnosing the worker selector against the live registry record.
## Pass 8 — 09:16Z (a6805a6): selector fixed, but `dispatchV1OwnerApproved` itself rejected the actl-managed record (`Unknown driverOptions key: 'actl'`). Root cause: the V1 branch never contained the certified B1.5 actl-bridge (it lives on `staging-merge`, fcee22a; the V0.2 driver runs from a separate checkout of that branch). PM merged `staging-merge` into the V1 branch (`61df4c7`; three additive conflicts in pm-judgment.ts resolved: AR-04 reconcile + decision evidence; all suites green; strict loader now accepts actl records).
## Pass 9 — 09:19Z (61df4c7): dispatch reached the actl bridge → `INPUT_STATE_UNKNOWN: Owner inputPermit factory is not installed` → Run FAILED, TASK-0001 FAILED (truthful canonical record kept). The orchestrator CLI must install the input-permit factory the certified driver installs. Fix routed (TASK-0058).
Note: the staging checkout `.jucontroler-staging/agent-relay-actl-bridge-20260913` carries an uncommitted 41-line dispatcher.ts addition (orphan-recovery decision request) not in any branch — left untouched; recorded here for the integration backlog.
## Pass 10 — 09:31Z (68bace9): no audit, no action — a Task FAILED at dispatch (no Result, no Delivery) was invisible to the loop (not READY-with-zero-runs, no pending Delivery, yet 'open'). Fix routed (TASK-0060 → 6d411d4): canonical `ensurePmDeliveryForFailedRun`, truthful 'FAILED before Result' packet, CHANGES/SAME_TASK → permission-checked `requestFailedRunTaskRetry`, budget → OWNER_REQUIRED.
## Pass 11 — 10:05Z (6d411d4): Delivery minted for the failed Run (pm-deliveries 0 → 1); PM final gate invoked and answered; judgment rejected `REJECTED_STALE: context_hash mismatch`. Pass 12 (10:06Z, no canonical change) → identical rejection → the failed-run packet's contextHash is not a pure function of canonical state. Fix routed (TASK-0061) together with the pre-existing red `test/v1-g4a-pm-delivery.test.mjs` on HEAD.
Canonical after passes 1–12: tasks 1 (FAILED, infra) / deliveries 1 PENDING / judgments 0 / qa 0. Every failure fail-closed; Founder actions 0; extra paid API 0.
## Passes 11–12 root cause (10:15Z): the failed-run packet hash IS deterministic (test + live double-build proven). The PM (mimo-v2.5-free) echoed the contract_hash into `context_hash` (both `077138fb…`), so the gate misreported a PM echo error as `REJECTED_STALE`. It also chose OWNER_REQUIRED for an infrastructure failure that the operator had already fixed. Fix routed (TASK-0062): explicit HASHES-TO-ECHO block + distinct placeholders, echo-error → bounded re-ask (real canonical move still REJECTED_STALE), retry-on-infra-failure guidance with remaining budget, disabled tool-id union (reviewer P2).
## Pass 13 — 10:24Z (727e5f1): MILESTONE — the PM final gate applied automatically: `CHANGES` on the failed-run Delivery (judgment persisted, Delivery ACKNOWLEDGED, retry preparation READY, retry Run 21dd0ce8 linked). Founder actions 0.
## Pass 14 — 10:26Z: pathology — the retry Run was reserved (runtime-binding RESERVED, fence 3, pane %16) but never sent; the loop then treated the fresh in-flight retry Run as 'failed before Result', minted a second Delivery, the PM issued a second CHANGES, a third Run (17b3a7bb) was reserved. Retry budget (max_pm_changes 2) consumed by the defect. Passes paused; fix routed (TASK-0063): detector must ignore in-flight retry Runs; retry Run must be sent through the permit-checked actl path; Task state truthful.
## Pass 15 — 10:37Z (969b772): in-flight guard works (no re-mint, no PM turn) but the two RESERVED-never-SENT retry Runs are not adopted → silent wedge (no audit). Fix routed (TASK-0064): adopt-and-send valid reservations through the permit path, release+re-dispatch expired ones, close out superseded dangling Runs, never silent.
## Pass 16 — 10:43Z (306048a): still silent — the two retry preparations are already consumed (dispatchedRunId set) and their leases were released, so adoption never reaches them and reconciliation audits nothing. Known gap kept open as WBS-9 backlog: 'retry prepared and reserved, lease expired before send' must be re-dispatched (or at least audited BLOCKED_RUNTIME, never silent). PM decision for the disposable project: cancel TASK-0001 through the canonical task-actions API with an explicit reason and let the loop bootstrap a fresh Task on the fixed code; the cancelled Task and its three Runs remain as the truthful record of the bring-up.
## Pass 17 — 10:45Z (306048a, --retry-blocked): still 0 s / no audit. FAILED is terminal in the state machine (cannot be cancelled), yet the loop keeps TASK-0001 as the open Task and never bootstraps; and it says nothing. Fix routed (TASK-0065): per-cycle audit line (never silent), terminal exhausted Tasks closed for the loop with TASK_EXHAUSTED, bootstrap packet lists them so the PM decides the replacement.
## Pass 18 — 10:52Z (2f8d83d): MILESTONE — `TASK_EXHAUSTED` audited for TASK-0001; PM bootstrap created the replacement `TASK-0002` (contract, READY) in the same pass; dispatch refused fail-closed: `actl target pane is not idle at its prompt` — actl `status` reports `inputState: INPUT_STATE_UNKNOWN` for seat %16 although the Codex prompt is visible. Diagnosing actl's Codex idle detection (banner lines '⚠ running async SessionEnd hook…' / 'You have 2 usage limit resets…' above the prompt are the visible difference vs the other seats).
## Pass 19 — 11:00Z (1b019a9): still refused — idlePromptMatches sliced the last 12 RAW lines of a pane whose lower 40 rows are blank (tmux capture returns the whole visible pane) → prompt never in the window. Fix: trim trailing blank rows first (cb876d7).
## Pass 20 — 11:05Z (cb876d7): MILESTONE — `dispatch-existing-ready → DISPATCHED TASK-0002 run f2949457` through the permit-checked actl path to Builder seat %16; the Codex Builder completed the work within ~1 min (scripts/check.mjs, docs/OPERATIONS.md, CHANGELOG.md; self-test PASS 2, exit 0). Founder actions 0. Cycle audit line present.
## Pass 21 — 11:07Z: the dispatch path is synchronous end-to-end — within pass 20 the Builder ran (~35 s), the Result was collected (`RUN_RESULT_RECEIVED`, result.md + agent-result.md), the automatic QA gate ran (QA-TASK-0002-f2949457: AC-01 command check PASS, AC-05 FAIL) → `finalQaStatus FAIL` → QA remediation preparation `QRP-QA-TASK-0002-…` minted (= 'QA CHANGES → SAME Task attempt 2' leg of the issue #6 chain). Pass 21: cycle IDLE — the orchestrator does not dispatch READY QA remediation preparations. Fix routed (TASK-0068).
## Pass 22 — 11:16Z (fe53ad3): QRP resume reached the dispatcher and was refused fail-closed: `runtime … is EXPIRED_HELD` — the Builder seat's reservation from the source Run (final collect) is only released by a PM judgment closeout in the V0.2 flow; the automatic QA path has no judgment before same-Task remediation. Fix routed (TASK-0069): canonical closeout/release of the source Run's seat before attempt 2 (idempotent, audited SEAT_RELEASED).
Also observed: QA AC-05 diffScope FAIL reports 'docs/, scripts/' because untracked directories are collapsed by git status (allowedPaths were the exact files) — evaluator should expand untracked dirs (`-uall`); queued as the next QA-side fix.
## Pass 23 — 11:22Z (0180c61): MILESTONE — `SEAT_RELEASED` (source run f2949457) → `QA_REMEDIATION_DISPATCHED` same-Task attempt 2 (run c33d25e3, remediation #1) → Builder fixed the change scope (staged exactly the 3 contract files; self-test PASS) → Result 2 collected → automatic QA `PASS` (QA-TASK-0002-c33d25e3, no failed criteria) → pending TASK_VERIFY Delivery `PMD-TASK-0002-c33d25e3…` minted; TASK-0002 RESULT_RECEIVED/VERIFYING. Founder actions 0. This is the observed chain `Builder attempt 1 → Result 1 → automatic QA → QA CHANGES → SAME Task attempt 2 → Result 2 → automatic QA PASS` from issue #6 WBS-10.
## Pass 24 — 11:24Z: MILESTONE — PM final gate auto-invoked on `PMD-TASK-0002-c33d25e3…` → `ACCEPT` APPLIED (judgment persisted, Delivery ACKNOWLEDGED, TASK-0002 → ACCEPTED) → bootstrap → PM decision `PROJECT_COMPLETE` ("GOAL-0001 completed via TASK-0002 ACCEPTED … all bounded scope delivered"). Cycle audit ACTED, openTasks []. The full issue #6 WBS-10 chain has now been observed on a real runtime: PM Task Contract → Builder attempt 1 → Result 1 → automatic QA (FAIL) → SAME Task attempt 2 → Result 2 → automatic QA PASS → PM final gate auto-invoked → ACCEPT → canonical transition → project stopped truthfully (PROJECT_COMPLETE). Founder intermediate routing 0, copy/paste 0, manual wake 0, extra paid API 0 (OpenCode free model + existing Codex/Claude seats).
Truthful notes: the goal text asked for one commit; the PM's contract did not encode a commit criterion, so the Builder left the three files staged (A) uncommitted — accepted per contract, recorded here. Every stage boundary in this run was crossed by a separate `--once` process (restart between Builder→QA, QA→PM, judgment→dispatch), which is live restart-safety evidence in addition to the drill suites.
## Passes 25–26 — 11:26Z: idempotency after completion — no new tasks/runs/deliveries/judgments. Pass 25 re-ran bootstrap and the PM re-decided PROJECT_COMPLETE (one extra PM turn: the completion decision is not yet cached — P2 follow-up: cache PROJECT_COMPLETE by contextHash like BLOCKED); pass 26 made no PM call.
