# Independent review #1 of ar/v1-pm (d6515a6..c670c38) — Claude Pro, 2026-09-16 15:17Z

PM disposition: FAIL accepted → CHANGES (P1: secret scrub too narrow; QA passthrough must never grant permissionMode — least privilege; P2: consistency-rule path matching anchor, stale comment, unconditional stderr file). Certification for run 3 waits for the fix + re-review.

V1PM_REVIEW: FAIL

P0: NONE

P1:
- **Secrets scrub regex is too narrow to support the claimed guarantee.** `src/backend/qa-semantic-evaluator.ts:116-122` (`workerOutputTail`) scrubs only three shapes — `sk-…` (`:119`), `Bearer <token>` (`:120`), and `password[:=]value` (`:121`) — before embedding the last 400 chars of raw worker stdout/stderr into the semantic-BLOCKED `reason` field, which IS a Founder/PM-facing surface (it flows into `QaAttemptRecord.reason`/final-gate packets, unlike the raw per-attempt files, see below). This misses generic `token=`/`apiKey=`/`api_key=`/`access_token=`/`secret=` key-value shapes, `Authorization: Basic <base64>` (only `Bearer` is covered), GitHub-style (`ghp_`/`gho_`/`github_pat_`) or AWS-style (`AKIA…`) key prefixes, and PEM private-key blocks. This is not hypothetical: the worker this scrub protects IS Claude Code CLI invoked with a real, existing `--claudeConfigDir` (round 24's own fix, `worker-registry.ts:98-102`) — i.e. a directory that genuinely holds Claude auth/session material — so a debug/verbose stderr dump from that CLI is a plausible real leak vector this regex set does not cover. **Where the raw file is written** is fine (verified separately, no P1 there): `attempt-N-stdout.txt`/`attempt-N-stderr.txt` (`qa-semantic-evaluator.ts:549-551`, unchanged/newly-unconditional) and the new `semantic-output-attempt-N.txt` (`:551`, written via `qaAttemptFolder`) both stay inside per-attempt evidence folders; `grep -rn "qaAttemptFolder"` across `src/` confirms no packet-builder or PM-facing code path reads either folder's contents back — only the scrubbed `reason` field is echoed further. The scrub-list gap is the real finding, not the write location.
- **QA passthrough now honors `driverOptions.claude.permissionMode`, and the LIVE `claude-code` QA worker record already sets it to `acceptEdits`** — a live, not hypothetical, regression against the wrapper's own documented invariant. `scripts/relay-worker-claude.mjs:662` still states in-comment "No --permission-mode flag (least privilege); QA only judges, never edits," but `:687` (`if (options.permissionMode === 'acceptEdits') claudeArgs.push('--permission-mode', 'acceptEdits');`, added in this range) now applies whatever `permissionMode` the caller passes to the QA `--print` invocation. That value is sourced from the worker registry (`qa-semantic-evaluator.ts:598` `const claude = worker.driverOptions?.claude;` → passed into `invokeOnce`, `:397-403`). Checked the real, live record this evaluator reads for V1CERT semantic QA: `~/.local/share/AgentRelay/data/_relay/workers/claude-code.json` → `"driverOptions":{"claude":{"permissionMode":"acceptEdits","configDir":"/home/skkse12/.claude-team"}}`. Before this diff, `runQaPrintPassthrough(prompt)` took no options and NEVER emitted `--permission-mode` for the QA path (confirmed via `git diff` — the old signature and old `spawn(claudeExe, ['--print', prompt], …)` call had no options param at all); after this diff, the exact same live worker record now causes every real semantic-QA invocation to run with `--permission-mode acceptEdits`, i.e. Claude auto-accepts file edits with no interactive confirmation available in `--print` mode. This directly undermines QA/Builder independence (the whole pipeline's own repeatedly-stated invariant that QA judges and never edits) — a semantic QA pass could now silently modify the very workspace it is supposed to be independently verifying, with no audit trail distinguishing an edit from a judgment. `test/v16-slice8-wrapper-qa-mode.test.mjs` even has a passing test named "QA passthrough applies permission mode and preserves prompt order" that asserts this AS a working feature, without any accompanying test that QA is denied write capability — the mechanism works exactly as coded, which is the problem. Recommend either hard-excluding `permissionMode` from the QA passthrough path regardless of registry content (restoring the documented "QA only judges, never edits" invariant), or correcting the live `claude-code.json` record's `permissionMode` and updating `worker-registry.ts` to reject `acceptEdits` specifically for `qa`-role-bound Claude workers.

P2:
- `src/backend/qa-semantic-evaluator.ts:410-424` (`inconsistentSemanticFail`) has a real but narrower over-broad-matching risk than the P1 above. The "does not exist" gate (`:415`, regex over English/Chinese/Korean non-existence phrasing) is not anchored to the specific clause naming the candidate path, and the path-candidate match (`:421-422`) is a loose `text.includes(candidate)` substring test against the covered checks' `detail` text plus the criterion's own description — not an exact path match. A compound semantic-FAIL reason that legitimately complains about something else ("X does not exist for edge case Y") while incidentally mentioning an unrelated, already-PASSED file's path could, in principle, be misclassified as `QA_INCONSISTENT` and bounced to a benign retry instead of surfacing as a real FAIL. This is narrower than the primary intended case (correctly scoped by `criterionId`, requiring ALL of that criterion's deterministic checks to be `PASS`, and restricted to `fileExists`/`diffScope` check kinds — `:417-420`), which is why this is P2 rather than P1: the mechanism is well-targeted for the evidenced failure mode (see `V1PM_REVIEW`'s check 6 below) and only loosely risks the adjacent case.
- `scripts/relay-worker-claude.mjs:662` — the stale "No --permission-mode flag... QA only judges, never edits" comment should be corrected or removed regardless of how the P1 above is resolved; as written it actively misdescribes the code three lines below it.
- `qa-semantic-evaluator.ts:551` now writes `stderr` unconditionally (previously gated on `outcome.stderr` being truthy) — harmless (an empty file instead of no file) but is a small, undocumented behavior change worth folding into the "outside the three items" note rather than leaving implicit.

TESTS_RUN: 59/0
Per-file (outer `node --test` totals; the two `v16-slice*` files each wrap a larger internal-assertion count that also reported 0 failures): `test/v1-orchestrator.test.mjs` 41/0; `test/v1-qa-loop.test.mjs` 6/0; `test/v16-slice3-semantic-qa.test.mjs` 1/0 (internal: 51/0); `test/v16-slice8-wrapper-qa-mode.test.mjs` 1/0 (internal: 12/0, including "QA passthrough applies permission mode and preserves prompt order" — see P1 above); `test/opencode-command-adapter.test.mjs` 10/0. `npx tsc -p tsconfig.server.json --noEmit` — clean, no errors.

| # | Check | Verdict | Evidence (file:line + revert-failing test) |
|---|---|---|---|
| 1 | Final-gate decision cache: no PM re-ask on unchanged `OWNER_REQUIRED`; invalidation on canonical change and `--retry-blocked`; no canonical write | PASS | `role-loop.ts:269-278` (`cacheFinalGateDecision`, only caches `outcome==='OWNER_REQUIRED'`, writes only to `cfg.stateFile` — never a `_relay/` path); `:657-660` (cache-hit check, `cached.contextHash === packet.contextHash`, returns before any adapter/PM work — genuine no-re-ask); invalidation is automatic on canonical change since `packet.contextHash` is recomputed fresh from current state every call; `:260-262` (`clearBlockedState` now also clears `state.finalGateDecisions`, wired to `--retry-blocked` via `main.ts`'s existing `--retry-blocked` path, unchanged in this range). Test: `v1-orchestrator.test.mjs`'s cache-audit/no-reask cases (part of the 41 passing). |
| 2 | Secrets: scrubbed tail cannot leak tokens/passwords; raw file location (attempt folder only, never audit/log) | **FAIL** | Location is correct (see P1 above, this sub-part verified clean: `grep -rn "qaAttemptFolder"` finds no downstream reader outside `qa-semantic-evaluator.ts` itself). The regex itself (`:119-121`) is too narrow — see P1. |
| 3 | Profile isolation: `configDir` validated (absolute, existing) in the strict loader; argv order (`--claudeConfigDir` before `--print`); wrapper passthrough applies it and still rejects unknown flags/`dangerously-skip-permissions`; `profileSource` truthful | **FAIL** | `worker-registry.ts:98-105` (`requireExistingAbsoluteDirectory`, real `fs.statSync` check) — VERIFIED. `qa-semantic-evaluator.ts:397-402` (`invokeOnce`, options array built before `'--print'` is appended) — VERIFIED. `relay-worker-claude.mjs:761-767` (`detectQaPassthroughArgs`, unknown flags throw `ArgError`) and `:126-131`/`:769` (both the wrapper's own `VALID_PERMISSION_MODES` check and `worker-registry.ts:376-383`'s enum check independently reject `dangerously-skip-permissions`) — VERIFIED, defense in depth. `semanticProfileSource()` (`qa-semantic-evaluator.ts:406-408`) truthfully reports `run-bound`/`inherited`/`cwd` matching the same three-way logic the wrapper's own `resolveClaudeConfigDir` uses — VERIFIED. Marked FAIL overall because "wrapper passthrough applies it" ALSO now applies `permissionMode: acceptEdits` to a live QA worker — see P1 above; this is the same mechanism the check asks about, just a consequence the check's own phrasing didn't anticipate. |
| 4 | Consistency rule: cannot mask a REAL semantic FAIL (only reclassifies when the FAIL reason asserts non-existence of a path covered by a PASSED `fileExists`/`diffScope`); no over-broad matching; bounded retries preserved; audit present | PASS_WITH_CONDITIONS | `qa-semantic-evaluator.ts:410-424` correctly scopes by `criterionId` + requires ALL of that criterion's deterministic checks to be `PASS` + restricts to `fileExists`/`diffScope` kinds (`:417-420`) — the primary intended case is well-targeted. `qa-gate.ts:394-403` still routes through the pre-existing `semanticBlockedAttempts < 3` bound (unchanged) — bounded retries preserved. `qa-gate.ts:395-401` (`recordRuntimeWarning`) — audit present. See P2 above for a narrower, non-blocking over-broad-matching risk in the "does not exist" gate + substring match. |
| 5 | Any new fail-open path, direct canonical write, or behaviour change outside the three items | **FAIL** | The `permissionMode`-to-QA-passthrough behavior change (P1 above) is exactly this: a real behavior change outside the cache/secrets/consistency-rule framing, with a live security-relevant consequence (QA gains real edit-acceptance capability). No new direct canonical write found anywhere in the range (`cacheFinalGateDecision`, `recordRuntimeWarning`, and all new file writes target either the local orchestrator state file or per-attempt evidence folders, never `_relay/tasks\|qa-attempts\|pm-deliveries\|pm-judgments` directly outside the existing kernel functions). |
| 6 | Hypothesis for the run-2 false negatives (cwd, tools, trust, timing) — evidence or UNKNOWN | Evidence-based hypothesis, not certain | Read `qa-semantic-runs/QA-TASK-0007-ac19f160…/attempt-1-stdout.txt` and the later `…3b05a9c0…` run's two attempts directly: both independently claim `docs/TROUBLESHOOTING.md` "does not exist" via an explicit stated methodology ("Independent verification via glob (`**/TROUBLESHOOTING.md`…) returned zero results", "not found by glob search across all `docs/` paths and the entire workspace") — i.e. the worker reports actually running a search, not merely guessing. Independently confirmed the file is real: `/home/skkse12/Desktop/Projects/Core/v1cert-workspace/docs/TROUBLESHOOTING.md` exists, mtime `2026-09-16 23:31`, BEFORE the first QA-TASK-0007 run at `23:32` — a genuine false negative, not a race. These runs predate this diff range (no `run-meta.json`/stderr files present — those are new in round 23/25). The `QA-TASK-0006` runs from the same window independently confirm the QA worker shares `~/.claude-team` — the same config directory the interactive Team persona uses — and genuinely hit that shared quota: `blocked-reason.txt` for two `QA-TASK-0006` attempts reads verbatim "You've hit your session limit · resets 11:30pm (Asia/Seoul)". Given the spawn's OS-level `cwd` was already set to the correct `workspaceRoot` even before this diff (unchanged `runProcess(…, cwd, …)` call), my hypothesis is that the false "does not exist" claims and the quota collision share the same root cause: a Claude Code CLI session/config directory shared with other live activity (Team persona and/or other QA-mode invocations against different task workspaces) can carry stale internal workspace/session-root state that process-level `cwd` alone does not override — which is also the most direct explanation for why round 24 (dedicated `configDir` isolation) and round 25 (explicit "ABSOLUTE WORKSPACE ROOT" line + required `cwd:` echo-and-verify in the prompt) are exactly the two fixes this PR ships. I could not independently confirm Claude Code CLI's own internal file-tool resolution mechanism (no access to its source), so I stop short of calling this fully proven — timing and "trust"-dialog-style causes were considered but I found no direct evidence for either (no trust-prompt text or timeout markers in any of the inspected `attempt-*-stdout.txt`/`blocked-reason.txt` files) — marking those two candidate causes UNKNOWN rather than asserting them. |
