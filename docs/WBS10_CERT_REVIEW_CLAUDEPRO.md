# Independent certification review of WBS-10 — Claude Pro, read-only, 2026-09-16 11:41Z

PM disposition: PASS_WITH_CONDITIONS accepted. CHAIN_OBSERVED YES, FOUNDER_ROUTING 0, PAID_API ZERO verified against canonical records. Three items the PM had listed as P2 are reclassified P1 by the reviewer (diffScope untracked-dir wrong FAIL; expired-lease retry silent in one state; PROJECT_COMPLETE re-asked every idle cycle) — all three are in the Builder fix already in flight (TASK-0071); certification will be proposed only after that fix is verified and re-reviewed, so that P0 = P1 = 0 holds. P2 corrections (defect count 17, g4a/g4c already green, raw registry fallback comment, 'restart-check' literal) folded into the report.

WBS10_CERT: PASS_WITH_CONDITIONS

P0: NONE

P1:
- Reclassified from the report's "Open P2 / known limitations" (not a defect newly introduced in `68bace9..0180c61`, but under-severity as disclosed): **diffScope evaluator collapses untracked directories, producing a wrong FAIL.** Live evidence: `qa-attempts/QA-TASK-0002-f2949457…/qa.json` `deterministic.checks[2]` — `criterionId AC-05, status FAIL, detail "허용되지 않은 경로가 변경되었습니다: docs/, scripts/", outOfScopeSample:["docs/","scripts/"]` — even though the Builder's diff exactly matched the contract's `allowedPaths` (the three files it was told to create). `git status` without `-uall` collapses new files inside new directories to the directory name, and the evaluator then treats that collapsed name as "not in `allowedPaths`" (which lists exact file paths). This is a reproducible wrong-FAIL for ANY Task whose first attempt creates files inside not-yet-tracked directories — not a one-off. Attempt 2 passed (`qa.json` for `c33d25e3…`: `AC-05 PASS`, `"changedCount":3`) only because by then `git status` reported the 3 files individually (their directories were no longer purely untracked-new after attempt 1's work) — i.e. it self-resolved by accident of git state, not because the evaluator was fixed. The report itself confirms no fix landed ("queued as the next QA-side fix"). Under Quality-Company-style severity (`founder/planning/QUALITY_COMPANY_CONTRACTS.md` §2), a reproducible wrong `FAIL` against a contract-satisfying diff is P1, not P2.
- Reclassified: **expired-lease retry re-dispatch remains genuinely silent for one specific state.** `docs/WBS10_RUNBOOK.md:35` (Pass 16, real, live): "the two retry preparations are already consumed (dispatchedRunId set) and their leases were released, so adoption never reaches them and reconciliation audits nothing... Known gap kept open as WBS-9 backlog." `docs/WBS58_EVIDENCE.md` commit `306048a`'s own message confirms this is still open at the end of the reviewed range: "expired-lease re-dispatch and superseded-run closeout still open (needs a canonical resume/closeout API)." For the *supervised* WBS-10 dry run this was caught and worked around by the PM operator (cancel + fresh bootstrap); for a genuinely **unattended** loop there is no such operator to notice a Task silently wedged with a consumed-but-undispatched retry preparation and zero audit line — exactly the "uncaught exception in place of a named terminal state" pattern the review charter asks to flag.
- Reclassified: **`PROJECT_COMPLETE` is re-asked, as a real LLM turn, every idle poll cycle with no cap.** Verified directly in `.jucontroler-staging/wbs10/audit/role-loop.jsonl` lines 22, 24, 26 — three separate `bootstrap`/`PROJECT_COMPLETE` audit entries at `11:25:09.863Z`, `11:25:56.675Z`, `11:26:00.711Z` (not two, see the P2 finding below), each with independently-worded PM reasoning text, i.e. three genuine fresh model calls in under a minute with the canonical state completely unchanged between them. The report's own disclosure calls this a caching nicety ("PROJECT_COMPLETE re-asked once per idle cycle until cached... P2 follow-up"), but for an **unattended** loop this is unbounded, uncapped LLM-call volume on every poll tick forever after project completion — on a rate-limited free-tier model (`docs/WBS10_RUNBOOK.md:37` already shows a real "usage limit resets" banner on this exact seat class) this is a real path to `BLOCKED_RUNTIME`/exhaustion with no backpressure, not merely an efficiency nit.

P2:
- **Report row "Open P2 / known limitations": `test/v1-g4a-pm-delivery.test.mjs` and `test/v1-g4c-pm-host-wake.test.mjs` are NOT currently red — MISMATCH.** Verified directly, five consecutive isolated runs: `node --test test/v1-g4a-pm-delivery.test.mjs` → `V1-G4-A Tests complete. Passed: 62, Failed: 0` every time, including the exact previously-failing assertion the bisect in `docs/WBS58_EVIDENCE.md` (CHANGES round 8) names — `test/v1-g4a-pm-delivery.test.mjs:270` `"J superseded D1 is reconciled to IGNORED"` — now `PASS`. `node --test test/v1-g4c-pm-host-wake.test.mjs` → `V1-G4-C Tests complete. Passed: 49, Failed: 0`. `git diff 0180c61..bf5a517 -- src test` is empty (docs-only commits after the reviewed range), and the reviewed range's own diff never touches the IGNORE-reconciliation logic these tests exercise, so I cannot attribute this to a specific fix commit — I can only confirm the CURRENT, deterministic, repeated result contradicts the report's "pre-existing... red... TASK-0070" claim. Recommend the PM re-verify and correct this row (and the "P0/P1 defects" row's parenthetical about it) before presenting to the Owner — the substantive news is good (nothing red), but the report is currently factually wrong about it.
- **Report's "Defects found... 16" is a miscount — 17 distinct semicolon-separated items are listed** (tools:{}, PM output contract, stuck session reuse, qa_route schema, validation convergence, builder selector, actl-bridge missing, input-permit factory, infra-failed run invisible, deterministic-hash misread, retry-run re-mint loop, unsent retry adoption, silent cycles/exhausted Task, actl idle semantics, trailing-blank-rows, QRP dispatch, EXPIRED_HELD seat). Cosmetic only.
- `src/backend/retry-dispatch.ts` `assertRetryWorkerRecord` (new in range, `git diff 68bace9..0180c61 -- src/backend/retry-dispatch.ts`) falls back to a raw `fs.readFileSync`+`JSON.parse` of the worker-registry file when the strict public loader rejects an actl-managed record, with its own inline check (`workerId` match, `role==='implementation'`, `driverOptions.actl.runtimeId`, `launchCommand` all required) before accepting it. This is the SAME already-disclosed drift `docs/WBS58_EVIDENCE.md` CHANGES round 5 named ("strict-loader/raw-record drift remains owner work"), now reused in one more place. Read-only, not a canonical write, and gated by its own non-trivial check — confirmed not a bypass of the real security-critical path (below). Still worth closing the strict-loader/raw-record drift generally, as already tracked.
- `retry-dispatch.ts`'s `adoptReservedActlRetry` passes the literal string `'restart-check'` as `currentSnapshotHash` to `obtainInputPermit()`. Traced this end-to-end: `obtainInputPermit` (`src/backend/actl-bridge.ts:733-747`) only checks the value is non-empty; the REAL identity/freshness proof is re-derived independently, live, inside the installed permit factory's `checkReady()` (`src/orchestrator/main.ts:156-181`), which always performs a fresh `actl status` call (`runtimeId` match, full `expectedContext` match, pane-identity presence, a freshly-read `currentSnapshotHash`, and — if not `inputState: READY` — a fresh `tmux capture-pane` idle-prompt check) regardless of what the caller passed. So `'restart-check'` is a vestigial/misleadingly-named placeholder satisfying an emptiness gate, not a security-relevant value — confirmed NOT a fail-open path. Worth a rename/comment for clarity, nothing more.
- `main.ts`'s `captureIdleSnapshot` (idle-prompt tmux check) is confirmed **explicit-socket + explicit-paneId only**: `execFileAsync('tmux', ['-S', path.resolve(socketPath), 'capture-pane', '-p', '-t', paneId, ...])` (`src/orchestrator/main.ts:48-55`) — argv form (no shell string), never an ambient/default tmux target. No finding here; recorded because the review charter specifically asked this be checked.
- Combined `node --test` run of the exact 7 files named in the review charter (default concurrency) produced 1 failure not present in any isolated or serial re-run — see `TESTS_RUN` below. Classified as a test-harness/live-dataRoot-lock-hygiene race under concurrent execution, not a product defect in the reviewed range (re-verified clean under `--test-concurrency=1` and per-file isolation). Traced the flagged artifact (`_relay/locks/actl-managed-controller.lock.d/holder.json`, holder `pid:2404085`) to a stale lock left by an already-exited process (`ps -p 2404085` → not running; the file itself no longer exists as of this review) rather than anything created by the code under review.
- `isFreeTierModel()`-style disabled-tool allowlist (`OPENCODE_TOOL_IDS_DISABLED`): my prior series review (`cp-wbs10-series-review.md`, range `9af10aa..68bace9`) flagged `apply_patch`/`execute` as present in the real OpenCode binary's tool-id set but absent from this allowlist (P2, not proven exploitable). **Confirmed closed in this range**: `git diff 68bace9..0180c61 -- src/integrations/opencode/command-adapter.ts` adds exactly `'apply_patch', 'execute'` to `OPENCODE_TOOL_IDS_DISABLED`. The allowlist remains a static list (not derived from a live `GET /tool/ids` call), which is the same general, not-newly-introduced completeness caveat as before — restated, not a new finding.
- `.jucontroler-staging/agent-relay-actl-bridge-20260913` still carries an uncommitted 41-line `dispatcher.ts` addition outside any branch (`docs/WBS10_RUNBOOK.md:27`) — inert integration backlog, unchanged from prior disclosure, not wired into anything in the reviewed range.

CHAIN_OBSERVED: YES
The full issue #6 WBS-10 chain (`PM creates Task Contract → Builder attempt 1 → Result 1 → automatic QA → QA CHANGES → SAME Task attempt 2 → Result 2 → automatic QA PASS → PM final gate auto-invoked → ACCEPT → canonical transition → authorized next Task OR truthful stop`) is independently confirmed against canonical V1CERT records, not merely the runbook's own narration — see the row-by-row table. `TASK-0001`'s three failed attempts (infrastructure defects, not the chain itself) and its `TASK_EXHAUSTED`/replacement path were also independently confirmed against canonical state and the audit log.

FOUNDER_ROUTING: 0 (Goal creation, role config file, worker record file, one local (non-canonical) orchestrator state-file reset at Pass 4, one session abort+rotation at Pass 2, and `--retry-blocked` CLI-flag usage — all six of the PM's disclosed manual actions are operator setup/technical-recovery actions on non-canonical or precondition state, not a Founder business/product decision or a mid-loop manual relay of information. Cross-checked against `.jucontroler-staging/wbs10/audit/role-loop.jsonl` (27 lines, full file read) and every canonical V1CERT record touched — no undisclosed manual canonical edit or Founder-routing event found.)

PAID_API: ZERO (role config `_relay/roles/V1CERT.json`, read directly: `pm.model "mimo-v2.5-free"`, `fallbackChain ["opencode/big-pickle","opencode/nemotron-3-ultra-free"]`, `zeroExtraBilling: true` on all three role assignments; builder = pre-existing actl-managed Codex seat `w-v1cert-codex-001`; qa = pre-existing `qa-worker:claude-code`. No new credential, no OpenAI API, matches the report exactly.)

TESTS_RUN: 65/1 (66 total, exact charter command, default concurrency — see P2: the 1 failure is a non-reproducing live-dataRoot lock-hygiene race, not a product defect)
Corroborating isolated/serial results (all read directly, this session): `node --test test/v1-orchestrator.test.mjs` alone → 35/0. Full charter command with `--test-concurrency=1` → 65/0 (0 failures). Every individual file in and around the charter list, run alone: `v1-builder-dispatch-drills` 5/0, `v1-orchestrator-drills` 8/0, `v1-orchestrator-actl-dispatch` 6/0, `v1-qa-loop` 4/0, `b15-fix-01-a` 1/0, `b15-fix-03-a` 1/0, `opencode-command-adapter` 10/0 — every one matches the report's claimed counts exactly. `npx tsc -p tsconfig.server.json` was not re-run in this review (not in the charter's required command list); the report's own `tsc` claims are carried over unverified by me.

---

## Row-by-row verification of `docs/WBS10_CERTIFICATION_REPORT.md`

| Report row | Verdict | Evidence (file:line / record id) |
|---|---|---|
| Runtime PM adapter / auth mode | VERIFIED | `_relay/roles/V1CERT.json`: `pm.runtimeAdapterId "opencode-command"`, `provider "opencode"`, `model "mimo-v2.5-free"`, `permissionProfile "read-only"`, `fallbackChain ["opencode/big-pickle","opencode/nemotron-3-ultra-free"]` — matches exactly. Tools-disabled claim confirmed by `OPENCODE_TOOL_IDS_DISABLED` in `src/integrations/opencode/command-adapter.ts`. Password-file-not-logged claim not independently re-verified this task (carried from my prior series review's check 8, which found no logging in the same code path). |
| Extra paid API dependency: ZERO | VERIFIED | Same role config; builder `runtimeAdapterId "actl-managed:w-v1cert-codex-001"`, qa `runtimeAdapterId "qa-worker:claude-code"` — pre-existing seats, no new adapter type introduced in `68bace9..0180c61`. |
| TASK_CONTRACT hash | VERIFIED | `V1CERT/_relay/tasks/TASK-0002/task.json` → `contract.contract_hash = "a7c48a6e81744cce36a38a9ccfc7e8e70613a37679c9a11e480acd96c42272cf"`, `contract_revision: 1` — byte-identical to the report's claimed value. |
| Builder attempt 1 | VERIFIED | Run folder `V1CERT/2026-09-16/worker-w-v1cert-codex-001/04`: `runtime-binding.json` `runId "f2949457-eb99-4481-a050-ed6bd8f45a6f"`, `collectStatus "FINAL_BOUND"`, `finalPacket.rawFinalText` (Codex's own completion text) names exactly `scripts/check.mjs`, `docs/OPERATIONS.md`, `CHANGELOG.md`; `result.md`/`agent-result.md` present in the run folder. |
| QA result 1 | VERIFIED | `_relay/qa-attempts/QA-TASK-0002-f2949457…/qa.json`: `finalQaStatus "FAIL"`, `failedCriteria ["AC-05"]`, `deterministic.checks[2]` = `diffScope`, `AC-05`, `FAIL`, detail names `docs/, scripts/` — exactly matches the report's own description, and confirms the P1 diffScope finding above (see P1 list). `remediationPreparationId "QRP-QA-TASK-0002-f2949457…"` present, matching the report's QRP mint claim. |
| Builder attempt 2 | VERIFIED | Run folder `.../05`: `runId "c33d25e3-84eb-4ff8-b29a-3a7736739b5e"`, `runtime-binding.json` `closeoutStatus "RELEASED"` on the SOURCE run (`.../04`) confirmed released before this dispatch (audit line `"step":"qa-remediation","outcome":"SEAT_RELEASED","runId":"f2949457…","reservationId":"rsv_1792…"` at `11:23:19.717Z`); `finalPacket.rawFinalText` confirms the Builder restaged exactly the 3 contract files. |
| QA result 2 | VERIFIED | `qa-attempts/QA-TASK-0002-c33d25e3…/qa.json`: `finalQaStatus "PASS"`, `failedCriteria []`, `deterministic.status PASS` (`AC-05 PASS`, `changedCount:3`), `semantic.status PASS` (`AC-03`, `AC-04` both PASS, `qaWorkerId "claude-code"`). `pm-deliveries/PMD-TASK-0002-c33d25e3…/delivery.json`: `kind "TASK_VERIFY"`, `status "ACKNOWLEDGED"` — pending-then-acknowledged, consistent with the report. |
| PM judgment | VERIFIED | `pm-judgments/PMJ-PMD-TASK-0002-c33d25e3…/judgment.json`: `decision "ACCEPT"`, `status "APPLIED"`, `appliedAt "2026-09-16T11:25:04.507Z"` — matches the report's claimed timestamp (11:25:04Z) exactly; audit line `{"step":"final-gate",...,"outcome":"APPLIED","decision":"ACCEPT","applied":true}` at the same timestamp. |
| Final Task state | VERIFIED | `tasks/TASK-0002/task.json`: `executionState "RESULT_RECEIVED"`, `pmState "ACCEPTED"` — byte-identical to the report's claim. |
| Next Task state: none, PROJECT_COMPLETE | VERIFIED (chain claim) / see P1 (re-ask cost) | Audit `{"step":"bootstrap","outcome":"PROJECT_COMPLETE",...}` at `11:25:09.863Z` immediately following the ACCEPT judgment, `openTasks: []` in the same cycle's summary line. The "truthful stop" claim is verified; the report's characterization of the re-ask cadence as a minor cached-completion nicety is disputed above (P1). |
| Restart / idempotency drills | VERIFIED (suite counts) / PLAUSIBLE (live-restart-boundary narrative) | All six suite counts (`v1-builder-dispatch-drills 5/5`, `v1-orchestrator-drills 8/8`, `v1-orchestrator 35/35`, `v1-orchestrator-actl-dispatch 6/6`, `v1-qa-loop 4/4`, `b15-fix-01/03 2/2`) reproduced exactly, isolated, this session — see TESTS_RUN. The live claim that "every stage boundary... was crossed by a separate `--once` process" is consistent with, but not independently provable purely from, the canonical records/audit timestamps I read (large inter-pass gaps are consistent with separate invocations, but I did not have a process-level trace of each individual `--once` launch) — marked PLAUSIBLE rather than VERIFIED for that specific sub-claim. |
| Founder intermediate routing: 0 | VERIFIED | See `FOUNDER_ROUTING` above — full 27-line audit log read, no Founder-routing event found; all disclosed manual actions classified as operator/setup, consistent with the report's own framing. |
| P0 / P1 defects (report claims 0/0 open for `9af10aa..0180c61`) | VERIFIED for NEW defects in `68bace9..0180c61`; see P1 above for RECLASSIFIED pre-existing items | Independent code-range review of `68bace9..0180c61` (`qa-gate.ts` closeout seam, `retry-dispatch.ts`, `main.ts` tmux capture, `role-loop.ts` failed-run detector, `pm-packets.ts`, `pm-delivery.ts`, `pm-judgment.ts`, `goal-task-runtime.ts`, `task-actions.ts`, `retry-preparation.ts`, `command-adapter.ts`) found no new direct canonical write outside an approved backend function, no new fail-open path, no new secret-logging path. My own prior series review (`cp-wbs10-series-review.md`, `9af10aa..68bace9`) independently found 0 P0/P1. Combined: 0 P0/P1 genuinely NEW in the full `9af10aa..0180c61` range — the report's headline number is accurate for "newly introduced." The three P1s I list above are RECLASSIFICATIONS of items the report itself already disclosed (as P2/known-limitations), not undisclosed defects. |
| P0/P1 defects — pre-existing red tests (`v1-g4a-pm-delivery`, `v1-g4c-*`, routed as TASK-0070) | **MISMATCH** — see P2 above | Both files pass cleanly and deterministically as of `bf5a517` (5 consecutive runs each for g4a; 1 run for g4c, no failures either): `V1-G4-A ... Passed: 62, Failed: 0`; `V1-G4-C ... Passed: 49, Failed: 0`. |
| Open P2 / known limitations | See P1/P2 above for reclassifications and one MISMATCH | Otherwise the disclosed facts (diffScope collapse, expired-lease gap, PROJECT_COMPLETE re-ask, static tool allowlist, uncommitted v02 dispatcher addition) are all independently confirmed true; only the SEVERITY labels and the red-test claim are disputed. |
| "Defects found... 16" | **MISMATCH (cosmetic)** | 17 distinct semicolon-separated items counted in the report's own list — see P2 above. |
| PM position (certification proposed only after this review returns no P0/P1, red tests fixed/accepted) | Procedurally sound; this review does not itself clear that gate | This review found the code range clean of NEW P0/P1 but is returning 3 P1-severity reclassifications of already-disclosed items plus one factual MISMATCH (red tests). Per the PM's own stated gate, `AGENT_RELAY_ROLE_ORCHESTRATOR_V1 = CERTIFIED` should not be proposed to the Owner until: (1) the report is corrected (g4a/g4c claim, defect count), and (2) the Owner is explicitly shown the diffScope/expired-lease/PROJECT_COMPLETE items as P1-for-unattended-operation, not buried as P2, so the Owner's certification decision is made on accurate severity information — hence `PASS_WITH_CONDITIONS`, not `PASS`. |
